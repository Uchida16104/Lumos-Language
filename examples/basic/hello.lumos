let message = "Hello, Lumos Language!"
print(message)

def greet(name) {
    return "Hello, " + name + "!"
}

let greeting = greet("World")
print(greeting)

for i = 1 to 5 {
    print("Count: " + str(i))
}
